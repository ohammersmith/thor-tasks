Some useful thor tasks.

* **git:stockpile [REPO]**

  Creates a clone of a git repository at [REPO] in a central location (`~/git-stockpile`).  For reference and for saving disk space by cloning locally to other places, i.e., vendor/rails.
                    
